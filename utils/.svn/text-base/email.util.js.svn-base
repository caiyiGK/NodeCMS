var nodemailer = require('nodemailer');

var user = '317387879@qq.com'
  , pass = 'caiyi_77!58'


var mailTransport = nodemailer.createTransport({
  	host : 'smtp.sina.com',
  	secureConnection: true, // 使用SSL方式（安全方式，防止被窃取信息）
  	auth : {
      	user : user,
      	pass : pass 
  	},
});
